# panels/chart/

Chart panel — Phase 1 default: GeckoTerminal embed (gratis, full features).
Phase 5 fallback: lightweight-charts native untuk speed.

- `chart.config.ts` — default settings (resolution, type).
- `ChartEmbed.tsx` — iframe wrapper dengan loading state.
- `Chart.tsx` — panel dengan header (token info), embed, switcher.
